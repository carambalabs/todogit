describe("-execute", () => {
  test("initializes firebase with the default config", () => {
    expect(1).toBe(2)
  })
  test("it resolves the promise when it finishes the setup", () => {
    expect(2).toBe(1)
  })
})